<%= form_with(model: onspecial, local: true) do |form| %>
  <% if onspecial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(onspecial.errors.count, "error") %> prohibited this onspecial from being saved:</h2>

      <ul>
      <% onspecial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer %>
    <%= form.text_field :customer, id: :onspecial_customer %>
  </div>

  <div class="field">
    <%= form.label :part %>
    <%= form.text_field :part, id: :onspecial_part %>
  </div>

  <div class="field">
    <%= form.label :start %>
    <%= form.date_select :start, id: :onspecial_start %>
  </div>

  <div class="field">
    <%= form.label :end %>
    <%= form.date_select :end, id: :onspecial_end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
